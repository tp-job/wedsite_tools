See also: [[Web Tools MOC]]

```excell
=A1+B1
```

```excell
=A1*B1
=A1/B1
```

Function
```excell
=SUM(A1:A10)
=AVERAGE(A1:A10)
=MAX(A1:A10)
=MIN(A1:A10)
=COUNT(A1:A10)
=COUNTA(A1:A10)
```

IF
```cmd
=IF(A1>=50,"ผ่าน","ไม่ผ่าน")
=COUNTIF(A1:A10,"ผ่าน")
=SUMIF(A1:A10,">=50")
=IF(E2>=80,"A",IF(E2>=70,"B",IF(E2>=60,"C","F")))
```

COUNTIF
```
=COUNTIF(A:A, "TEXT")
```
